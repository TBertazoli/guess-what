import useState from "react";

export default function SelectedGame(props) {
  const { selectedLevel } = props;

  //const [isModalOpen, setIsModalOpen] = useState(false);

  return (
    <>
      {/* attempt guesses */}
      {/* <div id="show-attempts">
        {showResults.map((counter, index) => (
          <h3 key={counter}>{counter.attempt}</h3>
        ))}
        {/* <span id="attempt-number"></span> */}
      {/* </div> */}
      {/* input field */}

      {/* show results */}
    </>
  );
}
